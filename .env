# ClawOps + Convos Environment Configuration
# ─────────────────────────────────────────────────────────────
# 
# CONVOS_MODE controls how the bridge connects:
#   http  = runs a local HTTP server on port 8002 (default, no wallet needed)
#   xmtp  = connects to real Convos via XMTP protocol (needs wallet key)
#
CONVOS_MODE=http

# ─────────────────────────────────────────────────────────────
# OPTIONAL: Real Convos / XMTP Integration
#
# To connect to a real Convos group chat:
#   1. Get your XMTP wallet private key
#   2. Paste it below (without quotes)
#   3. Change CONVOS_MODE=xmtp above
#   4. Restart the Convos bridge
#
XMTP_WALLET_KEY=
